import Badge from "@/src/components/Badge";

const metadata = {
  authentication: "Authentication",
  "toggle-input": {
    title: <span className="flex items-center justify-center gap-2">Toggle Input <Badge /></span>,
  },
};

export default metadata;